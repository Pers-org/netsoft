apiVersion: apps/v1
kind: Deployment
metadata:
  name: netsoft
  namespace: netsoft
spec:
  replicas: {{.Values.replicas}}
  selector:
    matchLabels:
      app: netsoft
  template:
    metadata:
      labels:
        app: netsoft
    spec:
      containers:
        - name: netsoft
          image: {{.Values.image}}
          ports:
            - containerPort: 80